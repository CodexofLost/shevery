# Issue #153 — Dhizuku starter ADB-TCP fallback (INVESTIGATION STATE(

Tracking: [#153](https://github.com/HmnDev-Tech/shevery/issues/153( — opened 2026-09-05, Xiaomi HyperOS/Android 16, v13.9-r32, **ADB mode**, Dhizuku as DO. No human reply yet (AI triage bot only(.



## Raw symptom (from issue log(

```
Direct Dhizuku execution did not publish binder, attempting ADB TCP 5555 activation via Dhizuku...
Failed to bind ADB to port 5555 via Dhizuku ✗
Starter command completed,but Shevery service did not become available ✗
```

User's read: **DO context shouldn't fall back to ADB-TCP at all** — it "always fails due to platform security restrictions on Device Owner APIs," and the doomed fallback masks the real failure.



## Working hypothesis (evidence-backed(

**H1 — `bindAdbTcp` is a structural dead-end as app uid.** (`DhizukuService.bindAdbTcp` runs `setprop service.adb.tcp.port 5555; setprop ctl.restart adbd` via `sh -c` from a user-service process = **app UID**. `service.*` props + `ctl.restart` require shell/root — SELinux denies on AOSP 8+ and certainly 14+/16/HyperOS. Polling `127.0..1:5555` then never goes live. ← matches `Failed to bind ADB … 5555`. (Evidence: `01-api.md`; the same call fails identically in `HomeActivity.bindTcp5555()` Dhizuku branch(**

**H2 — direct binder publish (step 6( commonly fails too.** `Starter.internalCommand` = native `libshizuku.so`, normally started as **shell uid** (`adb shell …( or **root** (`Shell.cmd(…(. Here it's `runCommand` = `sh -c` **as app uid** → binder prerequisites missing → `did not publish binder` is the *likely* case, not an edge. (Evidence: `Starter.kt`, `02-starter-flow.md`(.

**Consequence:** the whole "ADB-TCP-over-Dhizuku" fallback (l. 379-417( is a guaranteed triple-failure pathway: real error → 3 doomed steps → generic ✗. Bad UX regardless of whether direct publish could ever work.



## Fix directions (candidates, evaluation pending sub-agent analysis(

1. **Drop the ADB fallback in DO mode** (line 379 fork(: after direct-publish timeout, surface the true failure ("native server couldn't start as app uid — try ADB or root mode" — directly `postResult(DhizukuException(...(`. One doomed branch removed; error honesty restored. **Must mirror in `HomeActivity.bindTcp5555()`** (same `bindAdbTcp` call( + note `WatchdogManager` restart path.
2. **Make the direct path actually work** — pre-requisite for Dhizuku mode being worth anything: understand what `libshizuku.so` needs from its uid (binder, socket dirs, daemon props( and whether a DO-app-uid can be granted them (e.g. via Dhizuku's own elevated mechanisms, SELinux context, or running as a *shell* uid somehow(. Larger effort; do **not** block the #153 UX fix on this.
3. **Keep `enableAdb()`** (DPM op, legitimately works( but only as a real toggle, never as step toward 5555.



## Verification / test plan (no PC(

1. Repro #153 on the user's HyperOS device → capture full log (baseline(.
2. Apply fix → repeat → expect: no "attempting ADB TCP 5555" line; instead truthful failure text + the true error reason visible.
3. Home → bind TCP 5555 still shows its failure message (unchanged behavior( — confirm no regression in settings action.
4. Root/ADB-mode starters must be untouched (regression check on the normal paths(.



## Sub-agent task state

- [ ] `agent-ground-truth`: verdict on H1 (can `bindAdbTcp` ever succeed from app-uid?( and H2 (can native server start as app-uid?( — with evidence
- [ ] `agent-patch`: minimal diff-ready patch for `StarterActivity.startDhizuku()` + call-site audit (Home, Watchdog( + test plan
- [ ] owner (Landon(: reconcile verdicts → decide direction → reply #153 with findings
- [ ] owner: if fix rides r33, commit atop `fix/worker-tcp-mode-stale-port-mdns-fallback` (or dedicated branch( + update this doc's state section